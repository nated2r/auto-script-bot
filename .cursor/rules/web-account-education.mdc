---
description: 網頁「自定義規則」欄位命名、增量覆寫與 prompt 疊加
globs: "**/*.{tsx,ts,jsx,js,vue,html,css}"
alwaysApply: false
---

# 網頁自定義規則

設定頁／產稿表單的區塊名稱（UI 總稱＝**自定義規則**）：

| 區塊 | 內容 |
|------|------|
| 身份 | 領域、一句定位、目標觀眾 |
| 語氣 | 口語程度、禁用詞 |
| 必要有的 hashtag 跟 CTA | 固定 hashtag；必帶 CTA（含 C 型四句輪替若適用） |
| 要／不要 | 勾選偏好（留言領取、置入、恐嚇式等） |
| 口播文案參考區 | 使用者貼的對齊範本（語感／結構） |
| 禁止使用的題材 | 不要碰的主題／命中詞 |

另可有進階「自訂指令」自由文字。

## 增量覆寫（重要）

- **空白**＝不送進 prompt、維持系統原始設定  
- **有填**＝只覆寫該欄位對應行為  
- 不要對空欄位寫「（未填）」以免模型亂補偏好  

## Prompt 疊加順序

1. 系統硬規則（來自知識檔）＋網頁輸出格式底線  
2. 自定義規則（僅非空欄位）  
3. 本次輸入（新聞／參考口播）

提供「啟用／停用自訂指令」與範例填入按鈕為佳。
